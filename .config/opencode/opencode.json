{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "orchestrator",
  "enabled_providers": ["opencode", "openrouter"],
  "mcp": {
    "github": {
      "enabled": false,
      "type": "remote",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": {
        "Authorization": "Bearer {env:GITHUB_PAT_TOKEN}"
      }
    }
  }
}
